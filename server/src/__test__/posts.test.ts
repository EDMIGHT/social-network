describe('posts', () => {
  describe('get posts route', () => {
    describe('given the post does not exist', () => {
      it('should return a 404', () => {
        // TODO разобраться как работать с jest
        // ? и почему где только возможно он выдает ошибки
      });
    });
  });
});
